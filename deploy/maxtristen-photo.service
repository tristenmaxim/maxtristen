[Unit]
Description=maxtristen: photo (ecom image enhancer)
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/maxtristen/photo/backend
ExecStart=/opt/maxtristen/photo/backend/venv/bin/uvicorn main:app --host 127.0.0.1 --port 8001
Restart=on-failure
RestartSec=3

[Install]
WantedBy=multi-user.target
